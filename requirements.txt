# Core dependencies
python-dotenv>=0.19.0
tqdm>=4.62.0
openai>=1.0.0
faster-whisper>=0.9.0
python-dateutil

# Podcast feed and downloading
feedparser>=6.0.0
requests>=2.28.0

# Optional dependencies for CUDA support
# Uncomment if needed:
# torch>=2.0.0
# numpy>=1.24.0

# Optional GPU acceleration
torch>=2.0.0  # Only needed for GPU acceleration

# For development
pytest>=7.0.0
