# Core dependencies
python-dotenv>=0.19.0
tqdm>=4.62.0
openai==1.12.0  # Align with whycast-transcribe requirements
faster-whisper>=0.9.0
python-dateutil
markdown>=3.4.0  # For markdown to HTML conversion
whisper>=1.0.0  # OpenAI's Whisper library for transcription

# Podcast feed and downloading
feedparser>=6.0.0
requests>=2.28.0

# Optional GPU acceleration
torch==2.6.0  # Align with whycast-transcribe requirements
torchaudio==2.6.0  # Align with torch==2.6.0